name: my_profile_ketan
description: "An assessment for the jetDevs"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  very_good_analysis: ^5.1.0
  gap: ^3.0.1
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  image_picker: ^1.0.5
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
      - assets/images/


